name: core-build
on:
  push:
    branches:
      - 'master'
      - 'scratch/module-data-e2e'
  pull_request:

jobs:
  build:
    uses: Helias/reusable-workflows/.github/workflows/core_build_modules.yml@scratch/module-data-e2e
    with:
      module_repo: ${{ github.event.repository.name }}
      module_path: server-side/mod-bg-twinpeaks
      module_data: client-data.zip
